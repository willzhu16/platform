name: ci

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  ci: # job id MUST be `ci` — it prefixes the frozen check names (spec 01 §2.3)
    uses: willzhu16/platform/.github/workflows/ci-python.yml@v1
