name: security

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  security: # job id MUST be `security` — check-name contract
    uses: willzhu16/platform/.github/workflows/security.yml@v1
