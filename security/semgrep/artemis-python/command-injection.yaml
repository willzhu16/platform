rules:
  - id: artemis-py-subprocess-shell-true
    message: >-
      subprocess with shell=True runs the command through a shell; combined with
      non-literal input this is a command-injection risk. Pass a list of arguments and
      leave shell=False (the default).
    languages: [python]
    severity: ERROR
    metadata:
      category: security
      cwe: 'CWE-78: Improper Neutralization of Special Elements used in an OS Command'
      references:
        - https://owasp.org/www-community/attacks/Command_Injection
    # shell=True is only a risk when the command is built from something. A hardcoded
    # string carries no injection surface, and flagging it trains people to ignore the
    # rule — the same literal exclusion the JS rule uses.
    patterns:
      - pattern-either:
          - pattern: subprocess.call(..., shell=True, ...)
          - pattern: subprocess.run(..., shell=True, ...)
          - pattern: subprocess.Popen(..., shell=True, ...)
          - pattern: subprocess.check_output(..., shell=True, ...)
          - pattern: subprocess.check_call(..., shell=True, ...)
      - pattern-not: subprocess.call('...', ...)
      - pattern-not: subprocess.run('...', ...)
      - pattern-not: subprocess.Popen('...', ...)
      - pattern-not: subprocess.check_output('...', ...)
      - pattern-not: subprocess.check_call('...', ...)

  - id: artemis-py-eval-exec-nonliteral
    message: >-
      eval()/exec() on a non-literal value executes arbitrary code. Remove it, or use a
      safe parser (ast.literal_eval for literals, json.loads for JSON).
    languages: [python]
    severity: ERROR
    metadata:
      category: security
      cwe: 'CWE-95: Improper Neutralization of Directives in Dynamically Evaluated Code'
    patterns:
      - pattern-either:
          - pattern: eval($X)
          - pattern: exec($X)
      - pattern-not: eval('...')
      - pattern-not: exec('...')
