# Artemis shared gitleaks config (spec 02 §3.1).
# Extends the gitleaks default ruleset (broad, well-maintained coverage) with custom
# rules for the credential shapes this environment issues: Cloudflare API tokens, age
# private keys, GitHub fine-grained PATs, and ntfy topic URLs. A finding is a hard fail
# — rotate first, then purge history (security/README.md).

title = "Artemis platform gitleaks config"

[extend]
useDefault = true

[[rules]]
id = "artemis-age-secret-key"
description = "age private key (SOPS master key — losing/leaking this compromises every encrypted secret)"
regex = '''AGE-SECRET-KEY-1[0-9A-Z]{58}'''
keywords = ["age-secret-key-1"]

[[rules]]
id = "artemis-github-fine-grained-pat"
description = "GitHub fine-grained personal access token"
regex = '''github_pat_[0-9a-zA-Z_]{82}'''
keywords = ["github_pat_"]

[[rules]]
id = "artemis-cloudflare-api-token"
description = "Cloudflare API token (project-scoped deploy credential)"
regex = '''(?i)(?:cloudflare|cf[_-]?api)[^\n]{0,40}?['"=:\s]([A-Za-z0-9_-]{40})(?:['"\s]|$)'''
keywords = ["cloudflare", "cf_api", "cf-api"]

[[rules]]
id = "artemis-ntfy-topic-url"
description = "ntfy topic URL (anyone holding it can push alerts to subscribers — treat as a secret, spec 08)"
# The 8-char floor silently missed short real topics: `ntfy.sh/alerts` is a working
# topic and a working secret. Lowered to 3 and paired with an allowlist for ntfy's own
# static pages, which are documentation links rather than topics. Verified against
# gitleaks 8.30.1 (the pinned version): short and long topics are caught, /docs and /app
# are not. Covered by security/gitleaks-fixtures and the selftest gate.
regex = '''https?://ntfy\.sh/[A-Za-z0-9_-]{3,}'''
keywords = ["ntfy.sh/"]

  [rules.allowlist]
  description = "ntfy's own static pages are documentation links, not topics"
  regexTarget = "match"
  regexes = ['''https?://ntfy\.sh/(docs|app|login|signup|account|settings|pricing|static)(/|$)''']
